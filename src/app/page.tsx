import { Toaster } from "sonner";

const Home = async () => {

  return (
    <>
      <Toaster />
      {/* <Feed /> */}
      {/* {data.session === null ? (
        <LoginFooter />
      ) : ''} */}
    </>
  );
}

export default Home;